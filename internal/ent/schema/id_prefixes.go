package schema

const (
	// ApplicationPrefix is the prefix for all application IDs owned by Resource Provider API
	ApplicationPrefix = "reso"

	// ResourceProviderPrefix is the prefix for all Resource Provider IDs
	ResourceProviderPrefix = ApplicationPrefix + "pro"
)
